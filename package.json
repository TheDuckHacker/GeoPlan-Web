{
  "name": "geoplan-web-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for GeoPlan Web application, containing both the client and server.",
  "scripts": {
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run start\""
  },
  "workspaces": [
    "client",
    "server"
  ],
  "author": "GeoPlan Team",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "18.x"
  }
}
